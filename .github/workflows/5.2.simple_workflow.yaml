name: Workflow 5.2

on: [push]

jobs:
  log_group:
    runs-on: ubuntu-latest
    steps:
    - name: Créer un groupe de log
      run: |
        echo "::group::ecole-it"
        echo "hello, ecole-it"
        echo "::endgroup::"
        echo "ceci n'est pas dans le groupe"
